```latexsvg
\documentclass{standalone}
\usepackage{tikz}
\begin{document}
  \hspace{1.5cm}
% 绘制凹函数示例
\begin{tikzpicture}[scale=1.5]
    \draw[->] (-1,0) -- (2,0) node[right] {$x$};
    \draw[->] (0,-0.5) -- (0,2) node[above] {$y$};
    \draw[domain=-1.5:1.5, smooth, variable=\x, blue] plot ({\x},{\x*\x}); % 凹函数 y = x^2
    \coordinate (A) at (-0.5,{0.5*0.5}); % 取点 A
    \coordinate (B) at (1.5,{1.5*1.5}); % 取点 B
    \draw (A) -- (B); % 连接 A 和 B
    \node[below left] at (A) {$A(x_1,f(x_1))$};
    \node[above right] at (B) {$B(x_2,f(x_2))$};
    \draw[fill=black] (A) circle (1pt);
    \draw[fill=black] (B) circle (1pt);
    \coordinate (C) at ({(0.5 + 2)/2},{((0.5*0.5) + (2*2))/2}); % 线段 AB 中点
    \coordinate (D) at ({(0.5 + 2)/2},{((0.5 + 2)/2)*((0.5 + 2)/2)}); % 函数图像上对应中点横坐标的点
    \draw[fill=black] (C) circle (1pt);
    \draw[fill=black] (D) circle (1pt);
    \node[above] at (C) {Point on the line segment};
    \node[below] at (D) {Point on the function graph};
    \draw[red, dashed] (C) -- (D);
\end{tikzpicture}

% 绘制凸函数示例
\begin{tikzpicture}[scale=1.5]
    \draw[->] (-1,0) -- (3,0) node[right] {$x$};
    \draw[->] (0,-2) -- (0,0.5) node[above] {$y$};
    \draw[domain=-0.5:2, smooth, variable=\x, blue] plot ({\x},{- \x*\x}); % 凸函数 y = -x^2
    \coordinate (A) at (0.5,{-0.5*0.5}); % 取点 A
    \coordinate (B) at (2,{-2*2}); % 取点 B
    \draw (A) -- (B); % 连接 A 和 B
    \node[above left] at (A) {$A(x_1,f(x_1))$};
    \node[below right] at (B) {$B(x_2,f(x_2))$};
    \draw[fill=black] (A) circle (1pt);
    \draw[fill=black] (B) circle (1pt);
    \coordinate (C) at ({(0.5 + 2)/2},{((-0.5*0.5) + (-2*2))/2}); % 线段 AB 中点
    \coordinate (D) at ({(0.5 + 2)/2},{-((0.5 + 2)/2)*((0.5 + 2)/2)}); % 函数图像上对应中点横坐标的点
    \draw[fill=black] (C) circle (1pt);
    \draw[fill=black] (D) circle (1pt);
    \node[below] at (C) {Point on the line segment};
    \node[above] at (D) {Point on the function graph};
    \draw[red, dashed] (C) -- (D);
\end{tikzpicture}
  \hspace{1.5cm}
\end{document}
```
# 1 定义
设函数$f(x)$在区间$I$上有定义，若对于区间$I$上任意两点$x_{1}$，$x_{2}$，及任意$\lambda\in(0,1)$，都有$f(\lambda x_{1}+(1-\lambda)x_{2})\leq\lambda f(x_{1})+(1-\lambda)f(x_{2})$，则称函数$f(x)$在区间$I$上是凹函数；若都有$f(\lambda x_{1}+(1-\lambda)x_{2})\geq\lambda f(x_{1})+(1-\lambda)f(x_{2})$，则称函数$f(x)$在区间$I$上是凸函数。当等号仅在$x_{1}=x_{2}$或$\lambda = 0$或$\lambda = 1$时成立，函数是严格凹凸的。
# 2 用二阶导数判断
利用二阶导数判断首先，对函数$y = f(x)$求一阶导数$f^\prime(x)$。然后，对一阶导数$f^\prime(x)$再求导，得到二阶导数$f^{\prime\prime}(x)$。如果在区间I上，$f^{\prime\prime}(x)>0$，那么函数$y = f(x)$的图像在区间I上是凹的；如果在区间I上，$f^{\prime\prime}(x)<0$，那么函数$y = f(x)$的图像在区间I上是凸的